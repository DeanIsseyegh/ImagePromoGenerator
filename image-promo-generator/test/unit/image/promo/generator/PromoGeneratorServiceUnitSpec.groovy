package image.promo.generator

import grails.test.mixin.TestFor
import image.promo.generator.styling.IconUnicode
import image.promo.generator.styling.PromoIcon
import spock.lang.Specification

import java.awt.image.BufferedImage

@TestFor(PromoGeneratorService)
class PromoGeneratorServiceUnitSpec extends Specification {

	/*def "Generates promo image"() {
		given:
			PromoContainerBox promoContainerBox = Mock(PromoContainerBox)
			BufferedImage saleImage = new BufferedImage(100, 100, 1)

		when:
			BufferedImage bufferedImage = service.generatePromoImage(promoContainerBox, saleImage)
		then:
			bufferedImage
	}*/
}
